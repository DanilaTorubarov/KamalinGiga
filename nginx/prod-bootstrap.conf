# Bootstrap — только HTTP, для получения первого сертификата Let's Encrypt
server {
    listen 80;
    server_name physgraph.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
